rules_version = '2';

// Single-user demo app: every collection just requires being signed in as
// the one seeded DentiGO account. No public/anonymous access.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
